#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 * @x: has the value tio modify.
 * Return: the absolute value.
 */

int _abs(int x)
{
if (x < 0)
return (-x);
else
return (x);
}
