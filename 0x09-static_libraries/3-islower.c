#include "main.h"

/**
 * _islower - Verifies if a character is lowercase.
 * @c: Target character.
 *
 * Return: 1 if lowercase, 0 otherwise.
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
